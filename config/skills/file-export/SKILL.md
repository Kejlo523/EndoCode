# File Export

Category: Eksport
Local only: yes

## Summary
Eksport HTML, Markdown, ZIP, PDF-ready i paczek artefaktow.

## Agent Instructions
Dla eksportow tworz przewidywalne foldery wyjsciowe, manifest plikow i formaty latwe do sprawdzenia lokalnie. Nie nadpisuj niepowiazanych plikow.

## Local Runtime Rule
Use only local files, local model reasoning and approved local commands. Do not call cloud APIs unless the user explicitly adds such integration later.
