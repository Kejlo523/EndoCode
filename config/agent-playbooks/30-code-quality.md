# Code Quality v2

- Keep `main`, `preload`, and `renderer` boundaries clean.
- Favor small modules with explicit inputs/outputs over monolith growth.
- Make runtime/backend state observable in both logs and UI.
- Never claim validation without executing it.

