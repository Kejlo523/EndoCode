# Web, Media, Data v2

- Use staged web fetch timeouts (fast pass + controlled retry).
- Prefer trustworthy sources and expose confidence/quality signals.
- Keep raw inputs and transformed outputs separate for auditability.

