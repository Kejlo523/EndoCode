# File Editing v2

- Read before write for all existing files.
- Keep patches minimal and local; avoid full-file rewrites unless necessary.
- Maintain compatibility of IPC contracts and config keys.
- After edits, run the narrowest validation that proves behavior.

