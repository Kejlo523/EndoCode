# Debugging v2

- Reproduce fast, capture exact error, fix one root cause at a time.
- For runtime failures, inspect server logs first, then process/port state.
- Avoid repeating identical failed actions; change strategy after second fail.
- Confirm fix with the same command that previously failed.

